{% docs dim_calendar %}
Gold dimension model for calendar dates. Provides a clean calendar dimension for analytics use.

**Granularity:** One row per calendar day.

**Upstream:** `int_calendar`
{% enddocs %}
